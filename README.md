# 🎮 UE5 HUB — База знаний Unreal Engine

Веб-приложение, созданное как база знаний и сообщество для разработчиков на Unreal Engine 5. Проект выполнен в стилистике лаунчера **Epic Games Store**.

### 🔗 **[Открыть сайт (Live Demo)](https://asssite.onrender.com/)**

---

## ✨ Возможности

*   **📚 База знаний:** Подробные технические гайды по GAS, Nanite, Lumen, Blueprints и C++.
*   **🎨 Showcase:** Список популярных игр на UE5 с прямыми ссылками на Steam и Epic Games (с оригинальными иконками).
*   **💬 Форум:**
    *   Создание тем и обсуждений.
    *   **Загрузка картинок:** Возможность вставлять изображения прямо в текст постов и комментариев.
    *   Комментарии к гайдам и темам.
*   **👤 Пользователи:** Система регистрации и авторизации.
*   **💎 Дизайн:** Полностью темная тема, шрифт Inter, адаптивная верстка и UI-элементы в стиле Epic Games.

---

## 🛠 Технологии

*   **Backend:** Python 3, Flask.
*   **Database:** SQLite (с автоматической инициализацией).
*   **Frontend:** HTML5, CSS3, JavaScript (Fetch API для загрузки изображений).
*   **Deployment:** Render + Gunicorn.

---

## 🚀 Установка и запуск локально

1.  **Клонируйте репозиторий** (или скачайте архив):
    ```bash
    git clone https://github.com/ваш-юзернейм/ваш-репозиторий.git
    cd ваш-репозиторий
    ```

2.  **Создайте виртуальное окружение** (рекомендуется):
    ```bash
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # Mac/Linux:
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите проект:**
    ```bash
    python app.py
    ```
    *При первом запуске база данных `database.db` и папка для картинок создадутся автоматически.*

5.  Откройте в браузере: `http://127.0.0.1:5000`

---

## ☁️ Деплой на Render

Для запуска на хостинге Render.com используется `gunicorn`.

1.  Убедитесь, что в `requirements.txt` есть строка `gunicorn`.
2.  В настройках сервиса на Render укажите:
    *   **Build Command:** `pip install -r requirements.txt`
    *   **Start Command:** `gunicorn app:app`

---

## 📂 Структура проекта

```text
├── app.py                 # Основной файл приложения (Backend)
├── database.db            # База данных SQLite
├── requirements.txt       # Список зависимостей
├── static/
│   ├── style.css          # Стили (Epic Games Design)
│   └── uploads/           # Папка для загруженных картинок
└── templates/             # HTML-шаблоны
    ├── index.html         # Главная
    ├── games.html         # Игры
    ├── forum.html         # Список тем
    ├── forum_create.html  # Создание темы
    ├── forum_view.html    # Просмотр темы
    ├── guide.html         # Страница статьи
    ├── login.html         # Вход
    ├── register.html      # Регистрация
    └── about.html         # О нас
